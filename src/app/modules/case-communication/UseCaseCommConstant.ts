import React from 'react';

const UseCaseCommConstant = () => {
	let postDateFormat = 'MM/D/yyyy HH:mm';
	let momentFormat = 'DD.MM.YYYY HH:mm';
	let postDateHourMinSecFormat = 'MM/d/yyyy HH:mm:ss';

	return {
		postDateFormat,
		momentFormat,
		postDateHourMinSecFormat,
	};
};

export default UseCaseCommConstant;
