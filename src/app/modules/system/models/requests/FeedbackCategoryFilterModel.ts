export interface FeedbackCategoryFilterModel {
    feedbackCategoryName: string | null,
    feedbackCategoryStatus: string | null,
    feedbackTypeIds: string | null,
    userId: string,
    queueId: string
}