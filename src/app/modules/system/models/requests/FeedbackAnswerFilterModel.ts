export interface FeedbackAnswerFilterModel {
    feedbackAnswerName: string | null,
    feedbackAnswerStatuses: string | null,
    feedbackCategoryIds: string | null,
    userId: string,
    queueId: string
}